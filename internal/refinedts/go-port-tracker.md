# Go port tracker

One row per TS directory. Status moves pending → in-flight → ported;
`blocked-on` names imports a unit could not port past (from the
agent's report). Tests column is ported/passing from the report.
Update this file when a unit lands — it is the board the waves are
dispatched from.

**Integration stage landed:** `pnpm go:build`/`go:vet`/`go:test`
(equivalently `go build|vet|test -C refined-ts-go
./internal/refinedts/...`) are all GREEN across the whole tree —
`internal/refinedts/...` compiles, vets clean, and every test package
passes (0 failing tests). `gofmt -l` reports nothing. Resolved this
pass: every walk-integration-punchlist.md item except #4 (date_models
fidelity gap, informational only), #6 (checked — genuinely impossible,
see abstractdomain's ObjectAnnotationRef comment), #11 (control_flow's
.test.ts files, a separate porting task), and the parse_evaluator.ts
remainder of #13/#10 (a genuine port task, not mechanical). See the
narrowing, conditiontree, dataflow_facts, annotations, comparison, and
interprocedural rows below for what moved.

| TS directory | Go package | status | tests | notes / blocked-on |
|---|---|---|---|---|
| primitives | primitives | **ported** | 6/6 | int64 dyadics; FlowContext bypassed → `*checker.Checker` directly |
| refinement_sets | refinementsets | **ported** | 30/30 | GOOS rename (`repetition_window_forms.go`); RE2 lookahead by hand; set_simplification against a 3-method kernel interface; temporal grammar + integration tests await kernel_bridge/service |
| kernel_bridge | kernelbridge | **ported** | 33/33 (3 seam + 30 remainder) | cgo seam PROVEN (dlopen + locked worker thread; native-only, no wasm path); `structural`/`checkAssignability`/`encodeSpecification` NOT ported — Specification (object_graphs, unported) — blocked-on object_graphs; canonicalKeyOf's WeakMap memo dropped (map[string]any is not comparable in Go, and every call site rebuilds its wire form fresh anyway — no correctness loss, only the memo-hit speed win); question store salted to a DIFFERENT file name (questions-go-v1.json); tracing seam wired (wire.encodeSet/encodeTuple spans, kernel.ask span, cacheHit/cacheLookup/canonicalKey counters) |
| abstract_domain | abstractdomain | **ported** | 7/7 (grades.test.ts blocked — needs kernel_bridge, control_flow, dataflow_facts, annotations, service, not ported yet) | ObjectAnnotation (annotations/) referenced only by identity — stands in as an opaque `*struct{}` pointer; STRICTNESS (service/analysis_limits.ts) inlined as its current "full" value in TrustLevelAdmitted |
| dataflow_facts | dataflowfacts (+ walk for 2 files) | partial | 37/37 | alias_analysis (AliasClasses/ForgetPlaceEntries/UpdateTracked, against abstractdomain+silence) and linear_constraints (ConstraintsImply/MixedConstraintsImply/DecideComparison's kernel fallback, against kernelbridge.RefinedTSKernel.LinearImplies) PORTED earlier. INTEGRATION STAGE: the cross-package dataflowfacts↔narrowing cycle this row previously flagged is RESOLVED — condition_tree.ts's ConditionTreeOf/ConjunctiveLeaves/AllLeaves/ConditionTree/ConditionLeaf were pulled out of package narrowing into a new leaf package `internal/refinedts/conditiontree` (its own subtree, imports only ast), narrowing's three call sites (condition_tree_lowering.go, structural_narrowing.go, predicate_read.go) and walk's one (path_conditions.go) repointed at it, and narrowing/condition_tree.go deleted. DifferenceConstraintsOf, NegatedDifferenceConstraintsOf, LengthGuardNarrowings, and SumConstraintsOf now carry their REAL ported bodies (ConjunctiveLeaves-driven leaf walks, 1:1 against difference_constraints.ts/length_guard_narrowings.ts/sum_constraints.ts) instead of the "no rows" stub — go:build/go:vet/go:test all green. QuotientLow (length_guard_narrowings.ts, shared by walk's InverseFactorNarrowings) remains the one still-blocked function: it needs evaluation/number_range.ts's rangeOfSet, which is walk-owned (RangeOfSet, walk/number_range.go, 274 lines, too large to duplicate per PORT.md's "tiny pure helper" exception) and unreachable from dataflowfacts (walk imports dataflowfacts, not the reverse) — answers ok=false always, the sound fallback InverseFactorNarrowings' own productRow already tolerates. Still blocked, banners only: path_conditions.go's gateKeyOf/assumedVerdict/gatesTestedBy/correlationGateOf (now walk-owned, not dataflowfacts', per that file's own note); syntactic_facts.go's writtenNamesOf (needs service's resolvesToDefaultLib). EntryDependentConstraints and InverseFactorNarrowings stay in package walk (walk/entry_dependent_constraints.go, walk/inverse_factor_narrowings.go) as an earlier wave landed them, unaffected by this repoint. No .test.ts twins exist in the TS tree for difference_constraints.ts, length_guard_narrowings.ts, or sum_constraints.ts's SumConstraintsOf specifically (sum_constraints_test.go already covers SumPlacesOf/NoteSumExitConstraints/SumExitConstraintsOf) — nothing to port on the test side. |
| type_reading | typereading | **ported** | 11/11 | host reads direct on *checker.Checker; symbolAt inlined from service/program_resolution; exports.go gained GetConstraintOfType + SymbolInDefaultLib; jsnum.FromString for literal text |
| comparison | walk | **ported** | (folded into walk's count) | compareKnown() landed as walk/comparison_decision.go (CompareKnown) once FlowContext existed. INTEGRATION STAGE: the placeholder `internal/refinedts/comparison/` package (comparison_decision.go, never imported by anything outside itself) deleted per walk-integration-punchlist.md item 3 — this row's directory is retired. |
| silence | silence | **ported** | 7/7 | afterReaders/seededBinding ported against typereading.ArrivedUnchecked/ReadHostType and abstractdomain directly (the TS CheckerProgram/host param is `*checker.Checker`, per PORT.md); unknown.test.ts's whole-tree grep suite (no `return UNKNOWN` / no `UNKNOWN` import outside the allowlist) has no Go-shaped twin yet — ported the functional half (Residue/CutUnknown/DissolveUnknown each return the unknown atom) instead; see silence/unknown_test.go's file comment |
| narrowing | narrowing | **ported** | 22/22 | every leaf recognizer ported whole against `*checker.Checker` directly (comparison_leaf, string_test_leaf, number_test_leaf, array_shape_narrowing, instanceof_narrowing, typeof_ground, structural_narrowing, condition_tree, condition_tree_lowering, apply_narrowing, side_bounds, pinned_function, refine_predicate_reader, predicate_narrowings, narrowing_effects, type_guard_recognizers); type_guard_recognizers.SetNarrowKernel drops the TS source's setEngineKernel handover — blocked-on control_flow (wave 3), noted in the file; bound_condition.go and reassigned_names.go's FunctionWrites are the one real gap — blocked-on dataflowfacts.WrittenNamesOf, itself blocked-on service's resolvesToDefaultLib (a pre-existing dataflowfacts blockage, not new here) — every call site (condition_analysis.NarrowingsOf's bound-name pass, pinned_function's reassignment guard is unaffected since it only needs ReassignedNames) is annotated in place, no silent stubs; predicate_read.RecordUnreadGuard computes but never records (blocked-on assignability/decline_reasons.noteReason, wave 3); found and fixed a real bug in the port (a nil TrackedPlaceOf panic in the `\|\|`-literal-disjunction reading) via the ported test suite catching it live against the kernel. INTEGRATION STAGE: condition_tree.go's whole content (ConditionTreeOf/ConjunctiveLeaves/AllLeaves/ConditionTree/ConditionLeaf) EXTRACTED into a new leaf package `internal/refinedts/conditiontree` (see that row) to break a true two-way cycle with dataflowfacts; condition_tree_lowering.go, structural_narrowing.go, and predicate_read.go repointed at conditiontree.*, narrowing/condition_tree.go deleted (also added CopyBindingsOf/CopySourcePlaceOf to bound_condition.go — genuinely unported functions the walk-package integration needed at a live call site, both BLOCKED the same way ResolveBoundCondition already was: functionWrites needs dataflowfacts.WrittenNamesOf, itself blocked; both answer their sound "nothing proven" fallback). |
| conditiontree | conditiontree | **ported** | 0/0 (no .test.ts twin exists for narrowing/condition_tree.ts) | NEW leaf package, extracted from narrowing/condition_tree.go at integration (walk-integration-punchlist.md item 15): ConditionTreeOf/ConjunctiveLeaves/AllLeaves/ConditionTree/ConditionTreeKind/ConditionLeaf, imports only ast. Exists so dataflowfacts and narrowing — which import each other elsewhere in the Go port — can both read the shared condition tree without closing a cycle; narrowing's three call sites and walk's one (path_conditions.go) repointed here, and dataflowfacts' difference_constraints.go/length_guard_narrowings.go/sum_constraints.go now import it directly for their real (non-stub) bodies. |
| evaluation | walk | **ported** | 3/5 new (syntax_models_test, trig_reduction_test ported/passing — gated on the native kernel dylib, t.Skip if absent, never faked); reads.test.ts and evaluate_expression.test.ts NOT ported — both are `service/`-tier integration tests (formatRefinementAt+programFromSource, and check() respectively), which has no Go twin yet, same precedent as narrowing/condition_analysis_test.go's own "NOT PORTED" banner for check()-based cases; index_windows_test.go pre-existing | Per PORT.md's walk-package split, evaluation/ ports directly into `internal/refinedts/walk/`. This pass completed the LAST 8 files the prior wave left unported, plus the two partial files it flagged: array_method_models (readArrayIsArray/readArrayFrom/readArrayOf/readArrayReadMethods/readArrayWriteMethods), string_method_models (readStringMethods/readStringMatchWithConstRegex — RE2 has no lookaround, so `.match`/`.replace` against a literal regex translate source+flags to Go's regexp by hand via regexToGo, answering residue on a construct RE2 cannot express; UTF-16 code-unit primitives (utf16UnitsOf/utf16ToString/jsSlice/jsSubstring/codePointAtUTF16/jsPadStart/jsPadEnd) added since JS string reads are unit-indexed and Go's native strings are not), object_static_models (readObjectStaticValues/readObjectStaticMethods), coercion_models (readCoercionGlobals/readJsonMethods — jsStringToNumber/jsParseFloat/jsParseInt/jsURIFunction hand-transcribe the ECMA-262 grammars since Go's strconv diverges on hex/octal/binary prefixes and URI escaping; JSON.stringify serializes via a NEW hand-built jsonStringifyOf rather than encoding/json.Marshal, which SORTS map keys alphabetically and cannot reproduce a TS object literal's field order — walks AbstractValue.Keys directly, in order, per PORT.md's JSON-ordering rule), math_builtin_models (readMathBuiltin), schema_runtime_models (readSchemaRuntimeCall — PARTIAL: the `.decode`/`.encode` branch ports whole; the `.parse`/`.safeParse`/`.parseAsync` stated-result branch is BLOCKED on annotations/library_adapters/zod/parse_evaluator.ts's evaluateParseOutcome and annotations/worn_annotation.ts's wornOfAnnotation/wornOfObject, neither ported — callback_pins.go's SchemaCallbackPins already calls the same missing wornOfAnnotation per its own banner, so this file is not alone in waiting; the receiver-forget half runs and the function falls through to nil, the same fallback the TS caller's own unreached branches leave standing), sequence_copy_models (readSequenceCopyMethods), callback_call_models (readCallbackMethod). date_models.go and collection_models.go completed in place: readDateNow/readDateMethods (dateGetterWindows table, dateGetterOf over time.Time for the UTC calendar getters, jsToISOString) and readCollectionGetHas/readCollectionMethods (findCollectionEntry). No new checker/exports.go wrappers needed — every read went through already-exported methods (GetTypeAtLocation, GetSymbolAtLocation, SymbolInDefaultLib). `RecordCallSnapshot` (control_flow/call_site_snapshots.ts) remains the one call site in evaluate_call_expression.go blocked on a later control_flow stage — unaffected by this pass. |
| bindings | walk | **ported** | 0/0 (no .test.ts in bindings/) | Per PORT.md's walk-package split: assignments.go (WriteBinding/WriteElement/WriteProperty/LinkEmbedded/ForgetThrough), class_field_invariants.go (FieldInvariantsOf/InitialThisStateOf — the two symbols this_binding.go/this_property_access.go were already calling, unresolved, before this port landed), destructuring.go (ReadDestructuring; slotOf's `string \| number` union split into SlotOf(string)/SlotOfIndex(int) — no Go union twin). All three files landed clean against the walk package as it stood; a later concurrent wave landed capitalized exported forms of CheckAssignability/DeclaredOfKey/ElementOf/AnalyzeStatements/AssignedNames/AssignedNamesDirect that these files originally called lowercase (matching PORT.md's undefined-symbol convention at the time) — every call site was swept and updated to the capitalized names once they landed; see this row's report for the full list. |
| interprocedural | walk | **ported** | 0/0 (no walk-shaped test infra to port .test.ts files against yet) | Per PORT.md's walk-package split: bound_function.go, callback_element_outcome.go, callback_models.go (completes the pre-existing internal/refinedts/interprocedural/callback_models.go hub — see note below), callback_outcome.go, callback_pins.go, callback_recognition.go, callback_walk.go, callback_writes.go, callee_effects.go, contract_lookup.go, evaluate_call.go, function_summaries.go, inline_contract_body.go, inline_replay.go, inliner.go, kernel_summaries.go. contract_lookup.go's contractIndexes and callee_effects.go's overrideNamesHeld both key on `&ctx.Contracts` (the FlowContext field's address) rather than the map by value — a Go map value has no stable address across calls the way a WeakMap key on a JS object reference does. function_summaries.go's markerBySymbol/JoinSinkSummarized substitute a linear scan for structural equality (MarkerKey) in place of the TS WeakMap<AbstractValue, Symbol> keyed by object identity, since AbstractValue is a Go struct, not a pointer. kernel_summaries.go built a synthetic ReturnStatement node for a concise arrow body via `ast.NewNodeFactory(ast.NodeFactoryHooks{})` — the same pattern tsgo's own ls/printer packages use for synthetic nodes. The interprocedural/callback_models.go hub file (holding only the Callback type alias) became CONTENT-redundant once walk/callback_models.go had the full port. INTEGRATION STAGE: RESOLVED — Callback moved to a walk-local type alias in walk/flow_context.go, every `interprocedural.Callback` reference (bound_function.go, callback_models.go, callback_outcome.go, callback_recognition.go, callback_walk.go, inline_contract_body.go, inliner.go, flow_context.go) repointed to the unqualified `Callback`, and the now-empty `internal/refinedts/interprocedural/` package deleted (walk-integration-punchlist.md items 3 and 9). |
| control_flow | walk | partial | 5/5 (call_site_bindings_test.go, follow-on unit) | ~40 files ported into walk/ per PORT.md's "the walk package" split, snake_case names kept, no name collisions with evaluation/ or bindings/interprocedural files (checked before starting). answer_types, join_envs, tracked_bindings, assigned_names, certified_invariant, entry_env, ir_lowering_context, effect_expression, ir_assignment, ir_guard, ir_inline_call, ir_loop, lowering_to_kernel_ir, kernel_delegation, if_statement, iteration_element, loop_trip_count, loop_effect, loop_candidate, loop_push_growth, loop_exit_difference, loop_fixpoint, loop_statement, variable_statement, block_statement, return_statement, analyze_function, switch_statement, try_statement, analyze_statement, destructure_binding, call_site_snapshots, type_seed_answer, reach_to_token, cross_file_answer, env_row_answer, reach_answer, parameter_answer, flow_state_at_position — all ported. correlation_gate.go additionally ports gatesTestedBy/correlationGateOf from dataflow_facts/path_conditions.ts, which that file's own header explicitly left for this stage (not needed by evaluation/). NOT ported (this unit): call_site_bindings.ts (642 lines — CallSiteBindings/InitializeEnclosingCallbacks/CallSiteCtx referenced as cross-directory; CallSiteCtx itself IS defined in parameter_answer.go, a leaf shape needed immediately, same precedent as GateAssumption in flow_context.go — the eventual porter should reuse it, not redefine it), .test.ts files (assume_condition, call_site_bindings, dead_guard, entry_env, flow_state_at_position, kernel_delegation, loop_fixpoint, lowering_to_kernel_ir, switch_statement, walk_probe — all out of scope per this unit's instructions, which ported .ts files only).

**Follow-on unit (call_site_bindings.ts + its test):** LANDED — walk/call_site_bindings.go (CallSiteBindings, InitializeEnclosingCallbacks, plus unexported callbackSitePins/declaredJoin/declaredJoinUncached/reduceAccumulatorJoin) + walk/call_site_bindings_test.go (5/5 TS cases ported: exported-no-join, Array.from mapper pins, map pins match ArrayCallbackPins, transform pins match SchemaCallbackPins via a local surface stand-in for z.number(), non-exported join wears its call sites' union). CallSiteCtx reused from parameter_answer.go, not redefined, per that file's own banner. bindingsMemo substitutes the TS WeakMap<CheckerProgram, Map<FunctionDeclaration, Bindings|null>> with a mutex-guarded map keyed on the program pointer (same pattern as call_site_snapshots.go's snapshotStores) — a Go map's own present/absent distinction (the second `hasRemembered` return) stands in for the TS source's dedicated MISSING_BINDINGS sentinel, so no sentinel value was needed. SolveAccumulation (control_flow/loop_accumulation.ts) is a NEW symbol this file calls, not stubbed — that file was never ported by any earlier wave (not in the control_flow list above despite living in that directory; go-port-tracker had no row for it) and has no Go home yet; called as `SolveAccumulation(ctx *FlowContext, initial abstractdomain.AbstractValue, step func(abstractdomain.AbstractValue) abstractdomain.AbstractValue) abstractdomain.AbstractValue`, matching loop_accumulation.ts's own signature. Every other symbol this file needed (AbstractValueOfDeclared, ArrayCallbackPins/ArrayFromMapperPins/SchemaCallbackPins/BindParameter, ElementOf, CallSnapshotOf, SiteOf/AnalyzeToToken, symbolAt, AnalyzeStatement) was already landed by earlier waves. TS `try { ... } catch { ... }` (a refused kernel question mid-walk) ports as `func() { defer func() { recover() }() ; ... }()` IIFEs per PORT.md's kernel-panic convention, at the three sites the TS source wraps (the per-query walk to a callback's call, the visit-and-join over the callee body, and callbackSitePins' whole receiver-evaluate-and-pin tail). Symbols other stages must define (called here, not stubbed, per PORT.md's blocked-function convention): evaluateExpression (LANDED during this unit — walk/evaluate_expression.go), ReadDestructuring (LANDED — walk/destructuring.go), CheckAssignability (LANDED — walk/check_assignability.go, note VALUE not pointer target param), AssumedVerdict/gateKeyOf (LANDED — walk/path_conditions.go), InitialThisStateOf (LANDED — walk/class_field_invariants.go), CheckDependentReturn (LANDED — walk/dependent_return.go); still unlanded when this unit finished: AbstractValueOfDeclared (assignability/declared_value.ts), ContractOf/ProjectionSources (interprocedural/evaluate_call.ts), EntryDependentConstraints (dataflow_facts/entry_dependent_constraints.ts, called as dataflowfacts.EntryDependentConstraints — verify export lands with that exact name), DifferenceConstraintsOf/NegatedDifferenceConstraintsOf (dataflow_facts/difference_constraints.ts), LengthGuardNarrowings (dataflow_facts/length_guard_narrowings.ts), InverseFactorNarrowings (dataflow_facts/inverse_factor_narrowings.ts), SumConstraintsOf/NoteSumExitConstraints (dataflow_facts/sum_constraints.ts, path_conditions.ts), CallSiteBindings/InitializeEnclosingCallbacks (control_flow/call_site_bindings.ts, this unit's own deferred file). kernelSimplificationAdapter (loop_candidate.go) is a NEW small adapter satisfying refinementsets.SimplificationKernel's method-shaped interface over kernelbridge.RefinedTSKernel's struct-of-func-fields — that interface's own comment invited exactly this ("whatever package does port kernel_bridge can satisfy this interface directly"); reused from cross_file_answer.go, env_row_answer.go, parameter_answer.go, reach_answer.go, type_seed_answer.go rather than redefined at each site. |
| assignability (walk-half) | walk | partial | 2/3 (declared_value, refine_on_exact ported; dependent_return.test.ts blocked) | check_assignability, declared_value, dependent_edge, dependent_return, plain_sort, set_membership, worn_set_membership, sequence_measures, object_assignability, maybe_and_union, nan_wrapper, admitted_sort, temporal_membership, refine_on_exact, coverage_sentences ported into walk/ per PORT.md's "the walk package" split; the LEAF files (refinement_diagnostics, decline_reasons) already lived in package assignability and are imported one-way. comparison/comparison_decision.ts was already ported into walk/comparison_decision.go by a concurrent stage before this one landed (verified 1:1 against the TS source) — internal/refinedts/comparison/comparison_decision.go (the placeholder) becomes deletable at integration. Real bugs caught by re-reading the TS source after drafting: plain_sort.go's STRING_GROUND_FORM must key off refinementsets.Strings.Forms[0] (Star(Codepoints)), not Codepoints.Forms[0] (the TS `strings` import is Star(Codepoints), not Codepoints itself); admitted_sort.go's `admitted[known.KindTag]` needed an explicit primitives.Sort(known.KindTag) conversion — abstractdomain.PrimitiveKind and primitives.Sort are distinct named string types with identical literal values, which TS's structural typing hides and Go's nominal typing does not. temporal_membership.go's CalendarAsk seam needed a NEW adapter (calendarAskOf) translating refinementsets.CalendarQuestion (map[string]any, the TS `{op, ...}` shape) to kernelbridge.CalendarQuestion (a typed struct) — the two calendar layers ported independently under different shapes for the same wire message; covers every op refinementsets' own CalendarAsk closures pose (epochDays, compareDuration, validDuration). declared_value.go's abstractdomain.ObjectAnnotationRef (still `*struct{}` — its own package's port note not yet revisited) gets a per-*annotations.ObjectAnnotation memoized token (objectAnnotationRefOf) so identity comparisons (known.Stated == target's ref) hold across AbstractValueOfDeclared and object_assignability.go's checkObjectTarget. sequence_measures.go's MeasureReported takes `worn *abstractdomain.Measures` (not *annotations.Measures — the TS source's structural typing lets one function take either shape; Go needs the correctly-typed one, since both call sites pass known.Measures, an abstractdomain.Measures). dependent_return.test.ts (a full service/check.ts + kernel_bridge.ts integration test, `ignore: !kernelArtifactsPresent()`) is genuinely blocked — service/, surface/, and the control_flow/evaluation dispatchers that would run a real check are not wired end-to-end yet; no substitute test invented. codefix.test.ts and generic_positions.test.ts are NOT twins of any listed file (checked: neither imports any of the 15 ported files) and are out of this unit's scope. |
| annotations | annotations (+ walk for 2 files) | partial | 33/33 (schema_chain_compiler, chain_roots/root_constructor/method/numeric_method/args/case_stability, object_key_compiler/refine_rows/schema_compiler, object_specification, type_node_sets/aliases/utilities, annotation_of_type, annotation_file_facts, program_graph, interface_hash (sets-only), libraryadapters + libraryadapters/zod) | includes library_adapters/zod (ported, its own subpackage libraryadapters/zod); libraryadapters is a NEW leaf package + libraryadapters/compiledshape (a second leaf, structurally mirroring Annotation/Compiled) — required because zod_def_chain.ts/zod_def_roots.ts/zod_template_literal_root.ts call back into schema_chain_compiler.ts's isUnsupported/Annotation, a genuine two-way runtime dependency Go's no-cycles rule forbids keeping in one direction; chain_args.go's numberArg/stringArg/stringList duplicated verbatim into libraryadapters/zod for the same reason (noted in its header). INTEGRATION STAGE: contract_file_facts.ts and file_facts_compiler.ts LANDED — both needed FunctionContract, which now lives in walk (walk/flow_context.go), so per PORT.md's two-way-cycle rule (annotations already imports walk transitively nowhere but walk imports annotations, so annotations importing walk back would close the loop) they joined package walk instead: walk/contract_file_facts.go (CompileContractFileFacts) and walk/file_facts_compiler.go (CompileFileFacts, FileFacts, and interfaceHashOf's full contracts-folding body as InterfaceHashOf). annotations/interface_hash.go exported FormatStated/HashOf/SortStrings/PartsOfSets so walk's InterfaceHashOf could reuse the set/object formatting rather than duplicate it; InterfaceHashOfSets itself is now a thin wrapper over PartsOfSets+SortStrings+HashOf, unchanged behavior, its own tests still pass. Also fixed formatSetKey (interface_hash.go): now keys on the newly-landed kernelbridge.CanonicalKeyOfSet (order-free) with a kernelbridge.EncodeSet wire-string fallback for sets past the canonical-key budget, replacing the order-sensitive EncodeSet-only stopgap. worn_annotation.ts remains landed in walk (walk/worn_annotation.go, an earlier wave). Still blocked, NO FILE: parse_evaluator.ts (needs evaluation/js_value_conversion.ts, which IS ported as walk/js_value_conversion.go, but parse_evaluator.ts's own body reads deep into the zod adapter's compiled-schema shapes — a genuine port task, not a mechanical fix, left for a future unit; schema_runtime_models.go's `.parse`/`.safeParse`/`.parseAsync` branch and callback_pins.go's SchemaCallbackPins both already call the now-landed WornOfAnnotation/WornOfObject and are blocked on evaluateParseOutcome alone), the two zod .test.ts files (service/check.ts + hover_provider.ts, or the installed npm zod package directly — no Go-shaped twin). incremental_file_cache.go's programFacts remains unported (needs the coordinator to wire CompileFileFacts through the incremental cache, a separate task from this integration pass). |
| object_graphs | objectgraphs | **ported** | 5/5 | Specification/CardinalityPath/ObjectKey/ObjectNode/CountGroup ported 1:1 in graph_specification.go; derivedReaches ported 1:1 against kernelbridge.RefinedTSKernel.Transfer(TransferOpCountProduct) directly (no wire round-trip needed — the Go kernel interface already returns decoded TransferAnswer); unblocks kernelbridge's structural/checkAssignability/encodeSpecification (Specification now has a Go twin) — coordinator to wire those in kernelbridge/ next |
| surface | — | decide | — | z.* runtime surface — may stay TS-only (it is the user-facing library) |
| conformance | conformance | pending (last) | — | the parity gate: same fixtures, diff verdicts vs the TS checker |
| service | service | pending (last) | — | check entry, CLIs; the oracle/wire layer under service/tsgo does NOT port (deleted by design) |
| bench | — | decide | — | profilers are instrument code; port only what the gauge needs |

## Standing dispatch rules

- One agent, one directory, exclusive ownership; the only shared file
  is `internal/checker/exports.go` (adding wrappers) — collisions
  there are report items.
- Replace a misbehaving agent; never resume one alongside itself.
- Every report's insights fold into PORT.md before the next wave.
- Waves may run ahead of strict dependency order: agents port the
  independent parts and report blockages (no silent stubs).
